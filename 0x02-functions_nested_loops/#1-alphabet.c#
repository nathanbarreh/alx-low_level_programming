#include "main.h"

/**
 * main - a function that prints the alphabet, lowercase
 * 
 * Return: nada.
 */

void print_alphabet(void)
{
int l = 'a';

while (l <= 'z')
{
_putchar(l);
l += 1;
}
_putchar(10);
}
M-x
C-c
